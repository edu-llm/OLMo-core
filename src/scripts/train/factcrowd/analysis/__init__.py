"""
The analysis layer: the pre-registered trend test over collected endpoint scores, and the figures.

:mod:`factcrowd.analysis.trend` is pure arithmetic over numbers a scorer already produced, so it
imports no ``torch`` and can be run against a results file on a laptop.
"""
