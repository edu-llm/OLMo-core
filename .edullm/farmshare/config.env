# MixLaw 370M FarmShare profile.
EXPERIMENT_SLUG="mixlaw-370m"
INPUT_FAMILY="mixlaw"
STAGE_ROOT_REL="inputs/mixlaw"
DATASET_ID="pretrain/olmo-127b"
DATASET_VERSION="v1"
EVAL_REQUIREMENTS="requirements-mixlaw-eval.txt"
STAGE_EXTRA_ARGS=()
TRAIN_GPUS="${TRAIN_GPUS:-8}"
TRAIN_CPUS="${TRAIN_CPUS:-64}"
TRAIN_MEM="${TRAIN_MEM:-384G}"
TRAIN_TIME="${TRAIN_TIME:-72:00:00}"
STAGE_GPUS="${STAGE_GPUS:-1}"
STAGE_CPUS="${STAGE_CPUS:-8}"
STAGE_MEM="${STAGE_MEM:-32G}"
STAGE_TIME="${STAGE_TIME:-06:00:00}"
